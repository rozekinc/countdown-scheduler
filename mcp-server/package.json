{
  "name": "countdown-scheduler-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Local stdio MCP server exposing data-only tools over the countdown-scheduler repo's data/ directory, for use by an AI coding CLI.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "countdown-scheduler-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^6.0.3"
  }
}
